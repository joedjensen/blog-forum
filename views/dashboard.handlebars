{{#each posts as |post| }}
<div class="btn btn-primary d-flex justify-content-between mb-2"><span>{{post.name}}</span><span>{{format_date post.date_created}}</span></div>
{{/each}}
<div class="btn btn-primary d-flex justify-content-center">+</div>